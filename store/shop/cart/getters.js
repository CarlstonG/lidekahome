const getters = {
  checkoutId: state => {
    return state.checkoutId;
  },

  count: state => {
    return state.count;
  },

  checkout: state => {
    return state.checkout;
  }
};

export default getters;

